services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.3.3
    container_name: easydatasus-clickhouse
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      CLICKHOUSE_DB: default
      # Usuário administrativo usado apenas pelo entrypoint/bootstrap.
      # A aplicação deve usar easydatasus_ro, configurado em backend/.env.
      CLICKHOUSE_USER: ${CLICKHOUSE_ADMIN_USER:-easydatasus_admin}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_ADMIN_PASSWORD:-easydatasus_admin}
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
    volumes:
      - ./init_all_tables.sql:/docker-entrypoint-initdb.d/init_all_tables.sql
      - clickhouse_data:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8123/ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - easydatasus

  ollama:
    image: ollama/ollama:0.2.0
    container_name: easydatasus-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      OLLAMA_HOST: 0.0.0.0:11434
    networks:
      - easydatasus

volumes:
  clickhouse_data:
  ollama_data:

networks:
  easydatasus:
    driver: bridge
